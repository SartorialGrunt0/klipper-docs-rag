# klipper-docs-rag OpenAI-compatible proxy — generated by install.sh.
# Managed by install.sh; re-run install.sh to update, uninstall.sh to remove.
[Unit]
Description=klipper-docs-rag proxy (virtual model: klipper-expert)
After=network-online.target

[Service]
ExecStart=__EXECSTART__
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
